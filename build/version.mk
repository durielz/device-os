VERSION_STRING = 5.0.98

# PRODUCT_FIRMWARE_VERSION reported by default
# FIXME: Unclear if this is used, PRODUCT_FIRMWARE_VERSION defaults to 65535 every release
VERSION = 5098

CFLAGS += -DSYSTEM_VERSION_STRING=$(VERSION_STRING)
